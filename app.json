{
  "name": "Filter Bot",
  "description": "Filter Bot with Heroku Deploy",
  "keywords": [
    "telegram"
  ],
  "repository": "https://github.com/Tamilupdates/Filter-Bot-Heroku",
  "env": {
      "CONFIG_FILE_URL": {
          "description": "Config File URL",
          "required": true
      }
  },
  "buildpacks": [{
      "url": "heroku/python"
  }],
  "formation": {
      "worker": {
          "quantity": 1,
          "size": "basic"
      }
  }
}

